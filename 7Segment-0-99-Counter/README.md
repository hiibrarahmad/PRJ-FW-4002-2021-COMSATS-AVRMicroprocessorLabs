# 7-Segment 0–99 Counter

Drives two common-anode 7-segment displays (`PORTB`/`PORTC`/`PORTD` as segment drivers, via lookup tables) to count 0–99, wrapping back to 0.

![Proteus Simulation](assets/Counter-ProteusSimulation.png)
