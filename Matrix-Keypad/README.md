# 3x4 Matrix Keypad

Scans a 3x4 matrix keypad (row/column debounced read in `readkeypadalgo.c`) and writes the decoded digit out to `PORTC` (`writetoport.c`), driving 4 indicator LEDs.

![Proteus Simulation](assets/Keypad-ProteusSimulation.png)
